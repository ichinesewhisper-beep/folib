## 变更目标
- 将首页右上角的放大镜按钮从“打开弹窗”改为“跳转独立搜索页面”。
- 移除或停用快捷键弹窗逻辑，改为 `Cmd/Ctrl + K` 直接跳转到 `/<base_path>/search`。
- 不再维护弹窗组件，统一使用后端提供的高级搜索页面。

## 具体修改
1) 修改首页头部入口
- 文件：`OpenList-Frontend/src/pages/home/header/Header.tsx`
- 将原 `onClick={() => bus.emit("tool", "search")}` 改为跳转：
  - 使用 `<HStack as={LinkWithBase} href="/search"> ... </HStack>`，或在点击时 `location.href = base_path + "/search"`。
- 保留显示条件：`objStore.state === State.Folder` 且 `getSetting("search_index") !== "none"`。

2) 快捷键改跳转
- 文件：`OpenList-Frontend/src/pages/home/folder/Search.tsx`
- 移除弹窗相关逻辑（`createDisclosure`、`Modal` 等）。
- 保留键盘监听，将 `Cmd/Ctrl + K` 的处理从 `onToggle()` 改为：
  - `location.href = base_path + "/search"`（或 `to("/search")`）。
- 或者：直接删除该文件并相关引用，减少包体积（可选）。

3) 路由与页面
- 后端页面已在 `server/router.go:112` 注册 `GET /search`，模板位于 `server/handles/search_page.go`，前端无需改动路由。

## 先决条件
- 在“管理后台 → 索引”启用 `search_index`（建议 `database_non_full_text`）。
- 构建或更新索引提升体验。

## 验证
- 首页右上角点击放大镜直接进入高级搜索页面。
- `Cmd/Ctrl + K` 快捷键直接跳转，无弹窗出现。
- 高级页面的筛选、排重、分页与结果跳转正常。

## 代码参考
- 入口显示：`OpenList-Frontend/src/pages/home/header/Header.tsx:57–83`
- 后端页面：`server/handles/search_page.go`
- 路由：`server/router.go:112, 199`