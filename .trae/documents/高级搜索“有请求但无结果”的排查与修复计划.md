## 核心判断
- 请求已成功到 `/<base_path>/api/fs/search` 并返回 200；无结果多由索引未构建、过滤过窄、或搜索模式与关键字不匹配导致。

## 立即验证（无改动）
1) 查看搜索模式：GET `/<base_path>/api/setting/get?key=search_index`，确认不是 `none`。
2) 检查索引是否已构建：
   - POST `/<base_path>/api/index/build`；
   - GET `/<base_path>/api/index/progress`，确认 `is_done=true` 且 `obj_count>0`。
3) 检查过滤：
   - 取消“文件夹”勾选（`dir=0`），关键词改为空或常见英文/中文词（数字关键字在 Postgres 全文模式可能匹配不到）。
   - “指定类型”为空时不限定为仅文件；仅在填写扩展时（如 `pdf,mp4`）才限定文件。
4) 基路径与权限：GET `/<base_path>/api/me`，确认 `base_path` 为 `/` 或包含你要搜索的目录；结果会按基路径与可访问性过滤。

## 可能原因与对应处理
- 未启用或未构建索引：启用 `search_index`（推荐 `db_non_full_text` 先验证），执行构建。
- 使用 `db`（Postgres 全文）模式时仅数字关键字匹配弱：
  - 先用文本关键字验证；或切到 `db_non_full_text`（LIKE 模式）快速验证结果。
- 过滤过窄：URL 中 `dir=1` 仅搜索目录；如需文件结果，取消“文件夹”。

## 代码层说明（已准备好，无需你改动）
- 搜索页的范围逻辑已优化：只有填写“指定类型”时才限定为仅文件；勾选类型组不会收窄范围（当前模式不使用扩展过滤）。

## 预期结果
- 索引构建完成、使用合适关键字与范围后，列表应返回匹配项；若仍为 0，提供一次响应体 JSON，我据此继续定位过滤器或索引数据。